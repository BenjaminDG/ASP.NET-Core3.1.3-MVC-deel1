@model List<Filiaal>

<h2>Filialen</h2>
<ul>
    @foreach (var filiaal in Model)
    {
        <li>
            @filiaal.Id @filiaal.Naam @Html.DisplayFor(m => filiaal.Gebouwd)
            @Html.DisplayFor(m => filiaal.Waarde)
        </li>
    }
</ul>